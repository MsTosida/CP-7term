<!DOCTYPE html>
<html lang="ru">
<head>
    <title>CAST-XXTEA</title>
    <meta charset="utf-8"/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
          integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>
<body>
<h4 class="text-center mt-4 mb-4">Сравнительный анализ производительности и безопасности симметричных алгоритмов шифрования XXTEA и CAST</h4>
<main>
    <div class="container w-25">
            <form class="w-full" id="enc_form">
                <div class="form-group">
                    <label for="text">Оригинальное сообщение</label>
                    <input type="text" name="enc_text" class="form-control" required id="text">
                </div>
                <div class="form-group">
                    <label for="key">Ключ</label>
                    <input type="text" name="key" class="form-control" required id="key">
                </div>
                <button type="submit" class="btn btn-primary">Зашифровать</button>
            </form>
    </div>
    <div>   
        <p id="encResultXXTEA" class="mt-4"></p>
    </div>
    <div>
        <p id="encResultCAST" class="mt-4"></p>
    </div>
</main>
<script type="text/javascript">
    let form = document.getElementById('enc_form');

    form.onsubmit = async (event) => {
        event.preventDefault();

        const formData = new FormData(form);
        let jsonRequestData = {};

        for (let [key, value] of formData) {
            jsonRequestData[key] = value;
        }

        fetch('/encrypt', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(jsonRequestData)
        }).then(async (res) => {

            
            let paragraphXXTEA = document.getElementById('encResultXXTEA');
            let paragraphCAST = document.getElementById('encResultCAST');

            if (res.status === 200) {
                let result = await res.json();
                paragraphXXTEA.innerHTML = `
                <h3 class="text-center mt-4">XXTEA</h3>
                <h5 class="text-center mt-4">Зашифрование</h5>
                <p>${result.encryptedXXTEA} </p>
                <p>(${result.encodingTimeXXTEA} мс)</p>
                <h5 class="text-center mt-4">Расширование</h5>
                <p>${result.decryptedXXTEA} (${result.decodingTimeXXTEA} мс)</p>`;

                paragraphCAST.innerHTML = `
                <h3 class="text-center mt-4">CAST</h3>
                <h5 class="text-center mt-4">Зашифрование</h5>
                <p>${result.encryptedCAST} </p>
                <p>(${result.encodingTimeCAST} мс)</p>
                <h5 class="text-center mt-4">Расширование</h5>
                <p>${result.decryptedCAST} (${result.decodingTimeCAST} мс)</p>`;


            } else {
                paragraph.innerHTML = "Произошла ошибка"
            }
        })
    }
</script>
</body>
</html>