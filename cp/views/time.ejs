<!DOCTYPE html>
<html lang="ru">

<head>
    <title>CAST-XXTEA</title>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            border: 2px solid black; /* Граница таблицы */
            margin: 20px 0; /* Внешние отступы */
        }
        th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: left;
            vertical-align: top;
            width: 50%; /* Ширина столбцов */
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>

<body>
    <h4 class="text-center mt-4 mb-4">Сравнительный анализ производительности и безопасности симметричных алгоритмов
        шифрования XXTEA и CAST</h4>
    <main>
        <div class="container w-25">
            <form class="w-full" id="enc_form">
                <div class="form-group">
                    <label for="text">Оригинальное сообщение</label>
                    <input type="text" name="enc_text" class="form-control" required id="text">
                </div>
                <div class="form-group">
                    <label for="key">Ключ</label>
                    <input type="text" name="key" class="form-control" required id="key">
                </div>
                <button type="submit" class="btn btn-primary">Зашифровать</button>
            </form>
        </div>
        <div id="encResult" class="container w-600"></div>
    </main>
    <script type="text/javascript">
        let form = document.getElementById('enc_form');

        form.onsubmit = async (event) => {
            event.preventDefault();

            const formData = new FormData(form);
            let jsonRequestData = {};

            for (let [key, value] of formData) {
                jsonRequestData[key] = value;
            }

            fetch('/encrypt', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(jsonRequestData)
            }).then(async (res) => {
                let paragraph = document.getElementById('encResult');

                if (res.status === 200) {
                    let result = await res.json();

                    paragraph.innerHTML = `
                <table>
                    <thead>
                        <tr>
                        <th><h3 class="text-center mt-4">XXTEA</h3></th>
                        <th><h3 class="text-center mt-4">CAST</h3></th>
                        </tr>
                    </thead>
                <tbody>
                    <tr>
                        <td style="word-break: break-all; word-wrap: break-word;">
                                <h5 class="text-center mt-4">Зашифрование (${result.encodingTimeXXTEA} мс)</h5>
                                <p>${result.encryptedXXTEA} </p>                          
                        </td>
                        <td style="word-break: break-all; word-wrap: break-word;"> 
                                <h5 class="text-center mt-4">Зашифрование (${result.encodingTimeCAST} мс)</h5>
                                <p>${result.encryptedCAST} </p>
                        </td>
                    </tr>
                    <tr>
                        <td style="word-break: break-all; word-wrap: break-word;">
                                <h5 class="text-center mt-4">Расшифрование (${result.decodingTimeXXTEA} мс)</h5>
                                <p>${result.decryptedXXTEA} </p>                        
                        </td>
                        <td style="word-break: break-all; word-wrap: break-word;">
                                <h5 class="text-center mt-4">Расшифрование (${result.decodingTimeCAST} мс)</h5>
                                <p>${result.decryptedCAST}</p>
                        </td>
                    </tr>
                </tbody>
                </table>`;
                } else {
                    paragraph.innerHTML = "Произошла ошибка"
                }
            })
        }
    </script>
</body>

</html>